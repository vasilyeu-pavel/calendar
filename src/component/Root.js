import React from 'react';
import Header from './header';
import Main from './main';

const Root = props => {
    return (
        <div>
        	<Header />
        	<Main />
        </div>
    );
};

export default Root
